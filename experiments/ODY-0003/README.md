# ODY-0003 — Token embedding layer

| Field | Value |
| --- | --- |
| Phase | 3 |
| Date | 2026-07-28 |
| Purpose | Implement configurable Odyssey token embeddings |
| Result | **Successful** |

## Configuration

| Knob | Value |
| --- | --- |
| Vocabulary | 32,000 |
| Hidden size | 768 |
| Initialization | Xavier uniform |
| Padding index | 0 |
| Device | CPU (primary); CUDA when available |
| Dtype | float32 |
| Benchmark batch × seq | 8 × 128 |

Snapshot: `config.yaml` (from `configs/embedding.yaml`).

## Metrics

See `metrics.json` (generated by `scripts/benchmark_embeddings.py`).

| Metric | Expected order of magnitude |
| --- | --- |
| Parameter count | 24,576,000 |
| Weight memory (fp32) | ~93.75 MiB |
| Lookup | memory-bandwidth bound gather |

## Library layout

```
model/
├── embeddings.py
├── embedding_visualizer.py
├── initialization.py
└── config.py

math/
├── embeddings.md
├── tensor_shapes.md
└── …
```

```python
from model import load_embedding_config, OdysseyEmbedding
emb = OdysseyEmbedding(load_embedding_config())
x = emb(token_ids)  # (B, S, D)
```

## Strengths

- Config-driven shapes match Phalanx Runtime embedding gather semantics
- Explicit shape validation and padding_idx behavior
- Math notes document equations + PyTorch vs Phalanx paths

## Weaknesses

- No weight tying yet
- Visualizer PCA deferred
- CPU gather only in the logged baseline (GPU optional via `--device cuda`)

## Ideas for optimization

- Fuse gather with residual/RoPE later
- Quantized embedding tables for serving (Phalanx dequant path)
- Tie LM head when output projection lands

## Artifacts

- Plots / neighbors: `assets/embeddings/`
- Config snapshot: `config.yaml`
- Bench: `metrics.json`
- Inspect text: `inspect.txt`
